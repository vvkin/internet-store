{
    "name": "second",
    "version": "1.0.0",
    "private": true,
    "main": "./build/second/src/server.js",
    "scripts": {
        "pre-build": "rimraf ./build",
        "build": "yarn pre-build && yarn tsc -p ./tsconfig.json",
        "start": "node -r ts-node/register -r tsconfig-paths/register ./build/second/src/server.js",
        "lint": "eslint . --ext .ts,.js",
        "lint:fix": "yarn lint --fix",
        "valid": "yarn build && yarn lint"
    },
    "dependencies": {
        "dotenv": "^10.0.0",
        "fastify": "^3.24.0",
        "fastify-autoload": "^3.9.0",
        "fastify-plugin": "^3.0.0",
        "pg": "^8.7.1"
    },
    "devDependencies": {
        "@types/node": "^16.11.7",
        "@types/pg": "^8.6.1"
    }
}
